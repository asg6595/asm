; Процедура перевода цифры в соответствующий ей символ
; Вход:
; 	AL - шестнадцатиричная цифра
; Выход:
; 	AL - соответствующий ей символ

HexToChar			proc
					
					CMP AL, 10				; Если шестнадцатиричная цифра - десятичная цифра
					JB @@IsDigit
					
					ADD AL, 'A'-10			; Получаем код символа, соответствующий A - F
											; Символ = 'A' + (Цифра - 10)
					JMP @@EndProc
		
@@IsDigit:
					ADD AL, '0'				; Получаем код символа, соответствующий 0 - 9
					
@@EndProc:
					RET
					
HexToChar			endp