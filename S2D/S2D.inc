; Процедура, осуществляющую преобразование содержимого строки в десятичное знаковое число.
; Вход:
	; DS:ESI –адрес строки
; Выход:
	; EAX –код ошибки
	; 0 –нет ошибки
		; EBX –число
		; DS:ESI –указывает на первый символ после числа
	; 2 –пустаястрока
		; DS:ESI –указываетнасимволконцастроки
	; 3 –неправильныйсимвол
		; DS:ESI –указывает на него
	; 4 –слишком большое число(не из диапазона -2^31..2^31-1)
		; DS:ESI –неопределено

; Примечание: 
	; Число в буфере записано корректно, в следующих случаях:
		; · Перед числом допускается только любое количество пробелов
		; · Отрицательные числа начинаются с одного символа ‘-‘. Между ним и числом пробелыне допускаются.
		; · Число состоит из цифр ‘0’- ‘9’, при этом цифра ‘0’ может стоять на первом месте только учисла 0. Число ‘-0’считается недопустимым.
		; · Число должно заканчиваться разделителем. Разделителями считаются следующие символы: пробел, запятая и символы с кодами: 0, 10, 13.
		; · Числосодержится в диапазоне -2^31..2^31-1
StringToDec			proc
					
					call SkipSpaces
					cmp eax, 1
					je @@EmptyString					
					
					;call TestNumStart
					;cmp eax, 1
					;je @@IncorrectSymbol
					
					call StrToDec
					cmp eax, 1
					je @@IncorrectSymbol
					cmp eax, 2
					je @@IncorrectNumber
					jmp @@EndOfProc
					
@@EmptyString:
					mov eax, 2
					jmp @@EndOfProc
@@IncorrectSymbol:
					mov eax, 3
					jmp @@EndOfProc
@@IncorrectNumber:
					mov eax, 4
					jmp @@EndOfProc
					
@@EndOfProc:
					
					ret
StringToDec			endp